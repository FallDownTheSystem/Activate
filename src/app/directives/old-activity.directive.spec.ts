import { OldActivityDirective } from './old-activity.directive';

describe('OldActivityDirective', () => {
  it('should create an instance', () => {
    const directive = new OldActivityDirective();
    expect(directive).toBeTruthy();
  });
});
